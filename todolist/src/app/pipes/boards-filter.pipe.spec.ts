import { BoardsFilterPipe } from './boards-filter.pipe';

describe('BoardsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BoardsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
